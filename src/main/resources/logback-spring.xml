<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 复用 Spring Boot 的日志默认属性（颜色/异常渲染等） -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- 统一日志脱敏：手机号/身份证/银行卡/邮箱/地址（houbb 内置） + 秘钥（自定义扩展） -->
    <conversionRule conversionWord="sensitive"
                    converterClass="com.travel.config.sensitive.SensitiveMaskingConverter"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %sensitive%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 根日志级别，具体包级别仍由 application.yml 中 logging.level.* 覆盖 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
